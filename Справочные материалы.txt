this->setFixedSize(this->sizeIncrement()); Изменяет размер окна под размер виджетов
this->... Применяет метод к данному виджету
connect(tmp_widg(откуда идет сигнал), &ComboAn::add_new_abnt(что порождает этот сигнал), this(где находится слот), &FirstClass::add_new_widg(какой слот использовать));
this->adjustSize(); Задает размер данного виджета
Stop_btn->setContentsMargins(0,0,0,0); Размер виджета под размер содержимого(Текст, иконка и т.д.)

	connect(Stop_btn,&QPushButton::clicked,this,&QDialog::reject);
	connect(Accept_btn, &QPushButton::clicked, this, &QDialog::accept);
	void ComboAn::settings_btn_click()
{
	SettingsDialog sh;
	if (sh.exec() == QDialog::Accepted)
	{
	Действие, при нажатии подтверждения из диолгового окна
	}
}

if (sender() != NULL)
	static_cast<SettingsDialog*>(sender())->deleteLater(); Удаление при использовании динамического создания обьектов

down_v_lay->addSpacerItem(new QSpacerItem(40, 20, QSizePolicy::Expanding, QSizePolicy::Minimum)); Настройка местоположения обьекта
ActivateKeyboardLayout((HKL)MAKELANGID(LANG_ENGLISH, SUBLANG_DEFAULT), KLF_SETFORPROCESS);//Автоматически пишет на латинице

i%12 //Деление с остатком(в результат записывается остаток)
i/12 //Деление нацело

